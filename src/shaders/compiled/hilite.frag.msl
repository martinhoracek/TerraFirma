#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct buf
{
    uint hiliting;
    float amount;
};

struct main0_out
{
    float4 fragColor [[color(0)]];
};

fragment main0_out main0(constant buf& ub [[buffer(0)]])
{
    main0_out out = {};
    out.fragColor = float4(1.0, 0.800000011920928955078125, 1.0, ub.amount);
    return out;
}

